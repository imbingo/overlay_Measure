# SOMA Vision Metrology V2.1.0

## 主要更新

- 每个 Mark 的上层、下层分别支持多个手动 ROI，并按 `ROI 1、ROI 2...` 切换。
- 每个 ROI 使用稳定内部 ID；删除后显示编号可重排，但配方和测量引用不会串位。
- 分析当前 Mark 时一次处理全部 ROI，所有成功轮廓均可用于对位偏差、坐标系和几何量测。
- 对位偏差仍只由“基准轮廓 / 待测轮廓”决定，不引入主 ROI。
- 已选 ROI 被修改、删除或识别失败时，旧结果和选择立即失效，不会静默切换到其他轮廓。
- 批量测量、识别明细和 Excel 导出完整保留每次、每层、每个 ROI 的结果及稳定 ID。
- 旧版单 ROI Recipe 可直接加载并自动迁移。

## 验证重点

- 单图、双图分别创建多个混合类型 ROI。
- 保存并重新加载配方后确认 ROI ID 和基准/待测选择不变。
- 批量测量确认每次结果包含全部 ROI。
- 修改或删除已选轮廓后确认软件要求重新选择。
