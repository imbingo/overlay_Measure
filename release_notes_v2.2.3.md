# SOMA Vision Metrology V2.2.3

## 主要改动

- ROI 首次单击只选中，选中后可移动并使用控制点缩放；空白单击或 `Esc` 取消选择。
- 圆、圆环和卡尺圆新建时自动保持正圆；中键或空格加左键用于画布平移。
- ROI 拖动采用一次性提交，只让当前 ROI 的旧结果失效，并可通过一次撤销恢复。
- 坐标系具有可读名称、稳定编号、层、建立方式、旋转角、原点和引用要素说明。
- 坐标标注前会打开坐标系预览窗口，选择时同步高亮图上的坐标轴；无效或跨层坐标系不能确认。
- 安装目录和源码加入 120 μm 背光孔阵列上下层测试图、真实值和测试 Recipe。

## 测试数据

目录：`sample_data/v2_2_3_hole_array/`

下层相对上层的图像偏移为 X = +3.4 px、Y = -2.6 px。详细坐标及设备坐标预期值见 `hole_array_ground_truth.json`。
