# SOMA Vision Metrology V2.2.5

## 结果栏交互修复

- 四个结果表支持鼠标多选、`Ctrl/Shift` 扩展选择、`Ctrl+A` 和批量复制。
- 复制内容采用制表符格式，可直接粘贴到 Excel；右键可选择是否包含表头。
- 结果表始终保留横向滚动条，并按完整文字计算列宽；同时支持平滑拖动和 `Shift + 滚轮`。
- 工程模式支持右键单条或批量删除；生产模式只允许查看和复制。
- 批量结果按完整测量次数删除，并同步重算均值、3σ、PV及最终导出。
- 删除识别或几何结果后，相关对位、坐标系和尺寸结果同步失效，图像和 ROI 不受影响。
